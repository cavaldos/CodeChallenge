PORT_SERVER=5001

DB_HOST=113.173.72.198
DB_PORT=5432
DB_NAME=app_db
DB_USER=app_user
DB_PASSWORD=CodeChallenge2024

# JWT Configuration
JWT_SECRET=dev-secret
JWT_EXPIRES_IN=24h
PASSWORD_SECRET=password-secret
JWT_REFRESH_SECRET=dev-refresh-secret
JWT_REFRESH_EXPIRES_IN=7d

# CORS_ORIGINS: allowed origins (use host:* to allow all ports for a host)
# Examples: "localhost:5173,192.168.1.9:5173" or "192.168.1.9:*"
CORS_ORIGINS=localhost:5173

# Cookie sameSite: 'lax' for localhost, 'none' for cross-origin (IP addresses)
# COOKIE_SAME_SITE=none